// Stub: color-diff-napi native module not available in this build
// Returns null from render() so callers fall back to plain diff display
class ColorDiff {
  constructor(patch, firstLine, filePath, fileContent) {}
  render(theme, width, dim) { return null; }
  diff() { return ''; }
}

class ColorFile {
  constructor() {}
  highlight(theme) { return null; }
  render() { return null; }
}

function getSyntaxTheme(name) {
  return null;
}

module.exports = { ColorDiff, ColorFile, getSyntaxTheme };
