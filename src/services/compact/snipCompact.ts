// Stub: snipCompact is an internal Anthropic module not included in this fork.

export function snipCompactIfNeeded(..._args: unknown[]): { messages: unknown[]; tokensFreed: number } {
  return { messages: [], tokensFreed: 0 }
}
